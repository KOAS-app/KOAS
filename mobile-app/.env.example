# Local or production backend API URL
# For local development with Expo Go on the same machine, leave blank and the app will use the debugger host.
# For standalone builds, set this to your backend URL.
API_URL=http://localhost:5000
